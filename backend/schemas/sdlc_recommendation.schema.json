{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SDLC Recommendation Schema",
  "description": "Schema for validating SDLC model recommendations",
  "type": "object",
  "required": ["model", "why"],
  "properties": {
    "model": {
      "type": "string",
      "description": "The recommended SDLC model"
    },
    "why": {
      "type": "string",
      "description": "Explanation for why this model is recommended"
    },
    "when_not_to_use": {
      "type": "string",
      "description": "Scenarios where this model should not be used"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Confidence score for the recommendation (0-1)"
    }
  }
}