# Compatible with Python 3.10 - 3.13.
# Floors are used (not hard pins) so pip can resolve wheels for the
# running interpreter; older hard pins had no wheels on 3.13 / failed on 3.9.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
spacy>=3.8
sentence-transformers>=3.4
scikit-learn>=1.5
shap>=0.45
radon>=6.0
numpy>=1.26
scipy>=1.13
networkx>=3.3
